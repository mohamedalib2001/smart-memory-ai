## Packages
framer-motion | Essential for the complex loading animations and smooth transitions
lucide-react | Icons for the UI (already in base, but emphasizing use)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
